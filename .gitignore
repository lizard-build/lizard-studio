# Legacy in-repo host artifacts (install.sh now generates these in ~/.lizard-code/host).
# Kept ignored so older checkouts don't accidentally commit them.
src/host/host-config.json
src/host/launch.sh
src/host/host.log
src/host/shell-env.json

.DS_Store
